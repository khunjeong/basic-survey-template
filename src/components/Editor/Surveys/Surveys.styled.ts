import styled from 'styled-components';
import { colors } from '../../../styles/colors';

export const SurveyContainer = styled.div`
  width: 100%;
  border: 1px solid ${colors.BLUE_ORIGIN};
  padding: 16px;
`;
