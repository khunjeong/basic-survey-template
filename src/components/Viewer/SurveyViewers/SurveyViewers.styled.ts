import styled from 'styled-components';
import { Section } from '../../../components';
import { colors } from '../../../styles/colors';

export const SurveyViewerContainer = styled(Section)`
  border: 1px solid ${colors.GRAY_LIGHT_5};
  padding: 8px;
`;
